# 철근가공물량 산출 프로그램 — 인수인계 문서

## 프로젝트 목적
아버지(철근소장, 현장 근무)가 쓸 **철근가공물량 산출 프로그램**을 만드는 중.
회사에서 쓰던 비슷한 상용 프로그램(BarListPro)의 스크린샷을 참고자료로 받아서
그 구조/기능을 최대한 그대로 재현하려는 것이 목표.

## ⚠️ 이어서 작업할 때 반드시 할 일
**사용자가 이전에 채팅으로 보내준 BarListPro 스크린샷 사진들(철근 형상 대화상자 화면 2장 포함,
총 9장)을 이 대화(GPT)에 다시 첨부해달라고 요청할 것.** 이전 AI(Claude)는 그 사진들을
여러 번 참고했지만 텍스트 설명만으로 정확한 형상 아이콘을 재현하는 데 실패했음.
**사진을 직접 보면서 한 칸씩(행/열 위치별로) 사용자에게 확인받아가며 만드는 방식을 추천.**
절대 사진 없이 추측으로 형상을 만들지 말 것 — 이게 지금까지 가장 많이 반복된 실패 원인임.

## 기술 스택 / 파일 구조
- **`index.html`**: 프로그램 본체. 순수 HTML/CSS/JS 단일 파일 (프레임워크 없음). 오프라인 동작,
  `localStorage`로 자동저장, JSON 파일로 저장/열기.
- **`app.py`**: `pywebview`로 `index.html`을 감싸서 브라우저 주소창 없는 네이티브 창으로 띄우는 진입점.
- **PyInstaller로 exe 빌드**:
  ```
  python -m PyInstaller --onefile --windowed --name "철근가공물량산출" --add-data "index.html;." app.py
  ```
  결과물: `dist\철근가공물량산출.exe` (약 13MB, 설치 없이 더블클릭 실행)
- Python은 winget으로 설치: `winget install -e --id Python.Python.3.12`
  이후 `pip install pywebview pyinstaller`
- **GitHub**: https://github.com/parkjuyong-101/pyc (main 브랜치에 계속 push 중)

## 완성되어 잘 동작하는 것
- 프로젝트 구조: 공구 > 층(기초/지하/지상/옥탑) > 부재 > 전표 > 철근행 4단계 계층
- 새 프로젝트 생성(회사명/현장명/개시일/메모/층수 입력)
- 전표 추가/삭제, 철근 행 추가/삭제/위로/아래로
- **계산 공식 (실제 사용자 사진으로 검증 완료, 정확함):**
  - 총길이(mm) = 형상의 각 구간 길이 단순 합산 (각도는 계산에 영향 없음, 미리보기 모양에만 관여)
  - 합계 = 개수 × 개소
  - 실중량(kg) = (총길이/1000) × 합계 × 단위중량(kg/m), 단위중량은 KS D3504 이형철근 공칭 단위질량 사용 (D10=0.560 ~ D38=8.95 등, 코드 상단 `UNIT_WEIGHT` 참고)
  - 정착길이는 강종×규격별로 사용자가 직접 설정(`정착길이 설정` 다이얼로그)하는 조회값이며 계산에는 안 들어감(참고용 표시만)
  - 본수는 수동 입력 필드 (자동계산 아님, 원자재 몇 본 필요한지 사용자가 직접 판단해 입력)
- 인쇄 미리보기 (철근가공명세서 양식: 현장명/회사명/공구/부재/전표/작성일 + 표 + 합계)
- 저장(.rebar.json 다운로드) / 열기(파일 업로드)

## 아직 미해결 — 핵심 문제
**철근 형상 다이얼로그의 형상 아이콘 갤러리가 참고 사진과 다름.**
구조(일반철근/보조철근 탭 토글, 치수입력 패널에 A~G+각도X/Y/Z+반지름+호각도 12칸 고정 표시,
각도는 빨간색으로 구분해서 실제 입력 가능)까지는 사진과 맞춰놓았음.
**하지만 각 형상 아이콘이 실제로 어떻게 꺾이는지(정확한 절곡 방향과 구간 수)는 사진 없이
텍스트 기억만으로 재현하려다 계속 틀림.** 사용자가 이 부분 때문에 여러 번 다시 만들라고 요청했음.

현재 코드의 형상 데이터 구조 (`index.html` 안 `SHAPE_PRESETS` 배열, 대략 300번째 줄 부근):
```js
const SHAPE_PRESETS = [
  { id:'T1-00', name:'직선형', tab:'main', fields:['A'],
    segments:[{key:'A',angle:0}], sample:{A:5000} },
  // ... 이런 식으로 각 형상마다 segments(구간별 각도)와 fields(활성 입력칸) 정의
];
```
- `segments`: 순서대로 이어지는 구간들, 각 구간은 `{key:'A', angle:90}` (고정 각도) 또는
  `{key:'B', angleField:'angleX'}` (사용자가 입력하는 각도) 형태
- `tab`: `'main'`(일반철근) / `'stirrup'`(보조철근) / `'both'`(둘 다, 자유형만 해당)
- 총길이는 `computeShapeGeometry()` 함수가 구간 길이를 단순 합산해서 계산 (각도 무관)

**확실하게 검증된 것은 딱 2개뿐:**
- T1-00 직선형: A=5000 → 총길이 5,000mm
- T1-09 U형(양단갈고리): A=110, B=500, C=110 → 총길이 720mm

나머지 형상들은 사용자가 "임의로 만들지 말라"고 명확히 지적한 부분. **사진을 다시 보면서
정확한 구간 수와 꺾이는 방향을 하나씩 확인받아 다시 만들어야 함.**

## 작업 방식 관련 참고사항 (사용자 피드백)
- 사용자는 "이거잖아" "다르잖아" 같은 짧은 반응으로 명확한 불일치를 지적함 — 애매하면 반드시
  구체적으로 어느 부분이 다른지 되물을 것
- 형상 다이얼로그를 열 때 기본 탭은 **일반철근**이어야 함 (보조철근 아님)
- 각도는 미리보기 모양에만 영향, 총길이/무게 계산에는 영향 없음 — 이 점 사용자에게 이미 설명함
- exe 재빌드 시 반드시 실행 중인 이전 exe 프로세스를 먼저 종료해야 함 (파일 잠금 때문에 빌드 실패함)
